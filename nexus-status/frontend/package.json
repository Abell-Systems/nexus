{
  "name": "nexus-status-frontend",
  "private": true,
  "version": "0.1.0",
  "description": "Scientific Verification standalone frontend workspace (scaffolding phase under ADR 0024)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.11"
  }
}
